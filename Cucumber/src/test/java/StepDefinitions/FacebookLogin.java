package StepDefinitions;

import java.util.logging.Logger;
import org.openqa.selenium.WebDriver;

public class FacebookLogin extends AbstractPageStepDefinition{

static Logger log=Logger.getLogger(FacebookLogin.class.getName());
WebDriver driver=getDriver();


}

